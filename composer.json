{
    "autoload": {
        "psr-4": {
            "App\\": "app",
            "Tests\\": "tests"
        }
    },
    "scripts": {
        "test": "phpunit",
        "test:coverage": [
            "@putenv XDEBUG_MODE=coverage",
            "@test"
        ]
    },
    "require-dev": {
        "phpunit/phpunit": "^11.1"
    }
}
